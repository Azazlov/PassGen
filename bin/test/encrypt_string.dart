// import 'dart:convert';
// import 'encrypt_password.dart';

// Future<String> encryptString(String string, String psswd) async{
//   EncryptedPassword box = EncryptedPassword();
//   List<int> encryptedList = await box.getEncr(message: utf8.encode(string), passwd: utf8.encode(psswd));
//   String encrypted = base64Encode(encryptedList);
//   return encrypted;
// }

// Future<String> decryptString(String encryptedString, String psswd) async{
//   EncryptedPassword box = EncryptedPassword();
// }